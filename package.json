{
  "name": "glucosense",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build && esbuild server/index.ts --platform=node --packages=external --bundle --format=esm --outdir=dist",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "express": "^4.21.2",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "@hookform/resolvers": "^3.10.0",
    "@radix-ui/react-accordion": "^1.2.4"
    // coloque aqui todas as outras dependências que você usa no projeto
  },
  "devDependencies": {
    "vite": "^5.4.19",
    "esbuild": "^0.25.0",
    "typescript": "5.6.3"
    // e aqui todos os devDependencies que você precisa
  }
}
